# Postgres credentials — used by all 4 databases
DB_USER=postgres
DB_PASS=postgres

# JWT signing secret — must be the same value in auth-service and api-gateway
# Minimum 32 characters
JWT_SECRET=your-secret-key-minimum-32-characters-here
