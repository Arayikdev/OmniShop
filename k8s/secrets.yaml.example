apiVersion: v1

kind: Secret
metadata:
  name: omnishop-secrets
  namespace: default
type: Opaque
data:
  # base64-encoded values — run: echo -n 'yourvalue' | base64
  DB_PASS: <base64-encoded-password>
  JWT_SECRET: <base64-encoded-jwt-secret-min-32-chars>
